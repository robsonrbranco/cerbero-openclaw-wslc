# Segredos reais - nunca commitar (fiquem só em .env.example com placeholders)
.env
*.env
!.env.example

# Backups locais de scripts (podem conter valores hardcoded de sessões de debug)
*-bkp.ps1
*.bak

# Cruft de SO
.DS_Store
Thumbs.db

# Logs/estado gerados em runtime pelo watchdog - nao sao codigo, mudam a
# cada execucao (5 em 5 min), nao tem porque virar commit
logs/
