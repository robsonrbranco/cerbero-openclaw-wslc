# Patches aplicados em produção

Registro dos `openclaw config patch --file` aplicados direto no pod
(`/home/cerbero/.openclaw/openclaw.json` vive fora do git, no PVC
`cerbero-data`). Não são reaplicados automaticamente — são só documentação.

Ver a tabela completa com contexto de cada um em
[`LICOES-APRENDIDAS.md`, seção 24](../../LICOES-APRENDIDAS.md#24-mudanças-de-config-aplicadas-direto-no-pod-k3s--patches-salvos-no-repo-23-24072026).
